"use strict";

(function( $ ) {

	// var = {
	// 	init: function(element) {
	// 		if (!element.attr('title') )
	// 			return false;
	// 		var tooltip = $('<div>', {
	// 			class: 'cool-tooltip',
	// 			text: element.attr('title')
	// 		});
	//
	// 		return tooltip;
	// 	},
	// 	destroy: function(element) {
	// 		var title = element.data('title')
	// 	},
	// 	show: function(t) {
	// 		$('body').append(t)
	// 	},
	// 	hidden: function(t) {
	// 		t.remove()
	// 	},
	// 	move: function(t, event) {
	// 		var leftMove = e.pageX + 10;
	// 		var rightMove = e.pageY + 10;
	// 		tooltip.css({
	// 			left: leftMove,
	// 			right: rightMove
	// 		});
	// 	},
	// 	set: function() {},
	// 	get: function() {},
	// }
	// $.fn.toolTips = function() {
	//
	// 			this.each(function(index, element) {
	// 				element = $(element)
	//
	// 				element.hover(function() {
	// 					method['show']
	// 				}, function() {
	// 					method['hide']
	// 				})
	// 				element.on('mousemove', function(event) {
	// 					methods.move(element)
	// 				});;
	// 			});
	//
	// 			this.each(function(index, element) {
	// 				element = $(element)
	//
	// 				element.hover( function() {
	// 									/* Stuff to do when the mouse enters the element */
	// 									$('body').append(tooltip);
	// 									tooltip.fadeIn(300);
	// 									element.attr('title', '');
	// 				},
	// 				function() {
	// 					/* Stuff to do when the mouse leaves the element */
	// 					tooltip.fadeOut(100, function() {
	// 						tooltip.remove();
	// 					});
	// 				}
	// 			).on('mousemove', function(e) {
	// 				var leftMove = e.pageX + 10;
	// 				var rightMove = e.pageY + 10;
	// 				tooltip.css({
	// 					left: leftMove,
	// 					right: rightMove
	// 				});
	// 			});
	// 		});
	//
	//
	// };

})(jQuery);
